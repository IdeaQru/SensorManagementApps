<UserControl x:Class="SensorMonitorDesktop.Views.MonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="📊 Real-time Sensor Monitoring"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,15"/>

        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Group:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"
                       FontWeight="Medium"/>
            <ComboBox ItemsSource="{Binding Groups}"
                      SelectedItem="{Binding SelectedGroup}"
                      DisplayMemberPath="Name"
                      Width="200"
                      Margin="0,0,20,0"/>

            <Button Content="▶ Start"
                    Command="{Binding StartMonitoringCommand}"
                    Width="100"
                    Margin="0,0,10,0"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Padding="10,5"
                    BorderThickness="0"
                    Cursor="Hand"/>
            
            <Button Content="⏸ Stop"
                    Command="{Binding StopMonitoringCommand}"
                    Width="100"
                    Margin="0,0,10,0"
                    Background="#F44336"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Padding="10,5"
                    BorderThickness="0"
                    Cursor="Hand"/>

            <Button Content="🗑 Clear"
                    Command="{Binding ClearReadingsCommand}"
                    Width="100"
                    Background="#9E9E9E"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Padding="10,5"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>

        <!-- Data Grid -->
        <Border Grid.Row="2" 
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10">
            <DataGrid ItemsSource="{Binding LiveReadings}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      AlternatingRowBackground="#F5F5F5"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      RowHeight="35">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sensor" 
                                        Binding="{Binding SensorName}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Type" 
                                        Binding="{Binding SensorType}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Value" 
                                        Binding="{Binding Value}" 
                                        Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Unit" 
                                        Binding="{Binding Unit}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Time" 
                                        Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Status" 
                                        Binding="{Binding Status}" 
                                        Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="OK">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="WARNING">
                                        <Setter Property="Foreground" Value="#FF9800"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="ALARM">
                                        <Setter Property="Foreground" Value="#F44336"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="#263238"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusText}"
                       FontWeight="SemiBold"
                       Foreground="White"
                       FontSize="13"/>
        </Border>
    </Grid>
</UserControl>
