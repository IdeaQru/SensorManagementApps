<UserControl x:Class="SensorMonitorDesktop.Views.SensorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#1E88E5" />
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0" />
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Margin" Value="0,0,0,5" />
            <Setter Property="Foreground" Value="#424242" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Form Input -->
        <Border Grid.Column="0"
                Background="White"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="20">
            <StackPanel>
                <TextBlock Text="📡 Add / Edit Sensor"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,20"
                           Foreground="#263238" />

                <TextBlock Text="Sensor Group" Style="{StaticResource FieldLabel}" />
                <ComboBox ItemsSource="{Binding Groups}"
                          SelectedItem="{Binding SelectedGroup}"
                          DisplayMemberPath="Name"
                          Style="{StaticResource ModernComboBox}" />

                <TextBlock Text="Sensor Type" Style="{StaticResource FieldLabel}" />
                <ComboBox ItemsSource="{Binding SensorTypes}"
                          SelectedItem="{Binding SelectedType}"
                          DisplayMemberPath="Name"
                          Style="{StaticResource ModernComboBox}" />

                <TextBlock Text="Sensor Name" Style="{StaticResource FieldLabel}" />
                <TextBox Text="{Binding CurrentSensor.Name, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}" />

                <TextBlock Text="Address / Channel" Style="{StaticResource FieldLabel}" />
                <TextBox Text="{Binding CurrentSensor.Address, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}"
                         ToolTip="e.g., COM3, 192.168.1.100, Channel 1" />

                <Border Background="#F5F5F5"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,10"
                        Margin="0,0,0,15">
                    <CheckBox Content="✓ Sensor Active"
                              IsChecked="{Binding CurrentSensor.IsActive}"
                              Style="{StaticResource ModernCheckBox}"
                              Margin="0" />
                </Border>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="🆕 New"
                            Command="{Binding NewSensorCommand}"
                            Style="{StaticResource ModernButton}"
                            Width="100"
                            Background="#757575"
                            Margin="0,0,10,0" />
                    <Button Content="💾 Save"
                            Command="{Binding SaveSensorCommand}"
                            Style="{StaticResource ModernButton}"
                            Width="100" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Column="2"
                Background="White"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="📋 Sensor List"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="20,15,20,15"
                           Foreground="#263238" />

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Sensors}"
                          SelectedItem="{Binding CurrentSensor}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="40"
                          AlternatingRowBackground="#FAFAFA">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5" />
                            <Setter Property="Foreground" Value="#424242" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Padding" Value="15,10" />
                            <Setter Property="BorderThickness" Value="0,0,0,2" />
                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="15,10" />
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                        <DataGridTextColumn Header="Group" Binding="{Binding Group.Name}" Width="*" />
                        <DataGridTextColumn Header="Type" Binding="{Binding SensorType.Name}" Width="*" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*" />
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80">
                            <DataGridCheckBoxColumn.ElementStyle>
                                <Style TargetType="CheckBox">
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="IsHitTestVisible" Value="False" />
                                    <Setter Property="Focusable" Value="False" />
                                </Style>
                            </DataGridCheckBoxColumn.ElementStyle>
                        </DataGridCheckBoxColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
